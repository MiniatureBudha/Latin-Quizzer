import 'package:flutter/material.dart';

//This one will be on questions for the novellas (small books) if we decide to add them
class QNovellaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
